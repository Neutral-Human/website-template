// This is a config file for the website.
// You can change the settings(ex. api endpoint, website title) here.

const Config = {
    apiEndpoint: "",
    websiteTitle: "Template",
    mountTo: "#app",
}

export default Config